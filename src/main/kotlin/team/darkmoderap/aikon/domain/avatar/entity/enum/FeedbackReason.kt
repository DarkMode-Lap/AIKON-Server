package team.darkmoderap.aikon.domain.avatar.entity.enum

enum class FeedbackReason {
    FACE_SIMILARITY,
    STYLE_MATCH,
    BACKGROUND,
    DETAIL_BROKEN,
    AGE_MISMATCH,
    TOO_DIFFERENT,
    GOOD_QUALITY,
    BAD_QUALITY,
}
